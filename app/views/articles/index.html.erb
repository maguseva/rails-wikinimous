<article><% @articles.each do |article|%>
  <h2><%= link_to article.title, article_path(article) %></h2>
  <%= article.content %>
  <%= link_to "🗑", article_path(article), method: :delete, data: { confirm: "Delete #{article.title}?" }%><br><br>
  <% end %>
</article>

<p><%= link_to "+ Add a new article", new_article_path %></p>

